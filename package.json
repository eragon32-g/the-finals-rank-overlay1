{
  "name": "ranktag-finals-overlay-v69-clean-point4-full-preview",
  "version": "7.5.0",
  "private": true,
  "description": "RankTag removes old point 4, restores themes and full-width preview",
  "scripts": {
    "build": "node -e \"console.log('Build ok - public folder ready')\"",
    "start": "node -e \"console.log('Use Vercel to run this overlay')\""
  },
  "engines": {
    "node": "20.x"
  }
}