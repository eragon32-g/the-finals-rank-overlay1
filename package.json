{
  "name": "ranktag-finals-overlay-v53-generator-preview-lock",
  "version": "5.9.0",
  "private": true,
  "description": "RankTag locks Test Lab and Generator preview to the real iframe output",
  "scripts": {
    "build": "node -e \"console.log('Build ok - public folder ready')\"",
    "start": "node -e \"console.log('Use Vercel to run this overlay')\""
  },
  "engines": {
    "node": "20.x"
  }
}