{
  "name": "ranktag-finals-overlay-v90-layout-sync-hard-fix",
  "version": "9.2.0",
  "private": true,
  "description": "RankTag V90 hard-fixes premium layout JSON sync across editor, preview and generated link",
  "scripts": {
    "build": "node -e \"console.log('Build ok - public folder ready')\"",
    "start": "node -e \"console.log('Use Vercel to run this overlay')\""
  },
  "engines": {
    "node": "20.x"
  }
}