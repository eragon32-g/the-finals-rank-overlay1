{
  "name": "ranktag-finals-overlay-v60-ui-clean-accordion",
  "version": "6.6.0",
  "private": true,
  "description": "RankTag compact generator UI with collapsible sections",
  "scripts": {
    "build": "node -e \"console.log('Build ok - public folder ready')\"",
    "start": "node -e \"console.log('Use Vercel to run this overlay')\""
  },
  "engines": {
    "node": "20.x"
  }
}